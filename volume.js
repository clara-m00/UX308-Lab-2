// This program *calculates* the **volume** of a **cylinder**

let r = 6

let diameter = r/2

let height = 10

let volume = Math.PI * (diameter ** 2) * height;

console.log(`The volume of a cylinder with a diameter of ${diameter} and a height of ${height} is ${volume} m³`);